# ~/.profile: executed by bash(1) for login (interactive) shells.
# Usage: for things that are not specifically related to Bash, like
#   environment variables PATH and friends, and should be available anytime.
[[ -s ~/.bashrc ]] && . ~/.bashrc
